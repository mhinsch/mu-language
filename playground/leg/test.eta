
MyTempl[T @ type] @ class :
	{
	...
	}

//eq. to:

MyTempl @ class : 
	{
	T @ type
	}->
	{
	const_member_fn (., i @ int) @ int =
		;return (.aValue)
		
	n_const_member_fn (.!, i @ int!) @ () =...
	
	static_fn (bla @ int) @ int =...
	}

count (s @ string) @ int =
	;return s.length

//eq. to:

count @ string -> int =
	{
	-> s @ string			//????
	
	return s.length
	}
	
(f, j) @ int = (), 1
g @= obj

a @= 0;; b @ float = 3.2


main(args @ vec[string]) @ int =
	{
	(x, y) @ int = 3, b
	
	"a"
	
	b @=
		;a 3
	
	y @= z * ((r @ int, q @ float)->{return r.float * q}) (1, 2.5)
	
	while: { a>0 },
		;print a
	
	a:   b c  +  d
	
	a = $$ + 10
				
	c @= {out 4}
	}

